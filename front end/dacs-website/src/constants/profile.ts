/**
 * Default profile picture for accounts that have not uploaded their own
 * photo: the Dominant Asia logo. The John Doe farmer photograph
 * (/images/user-profile.jpg) belongs to the mock account only — never use
 * it as a generic fallback.
 */
export const DEFAULT_AVATAR_URL = "/images/profile/default-dominant-avatar.png";
